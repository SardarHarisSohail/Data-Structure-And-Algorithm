def find_maximum(arr):
    max_value = arr[0]
    for num in arr:
        if num > max_value:
            max_value = num
    return max_value

# Example usage
arr = [3, 1, 4, 1, 5, 9, 2]
print("Maximum value:", find_maximum(arr))  # Output: 9
